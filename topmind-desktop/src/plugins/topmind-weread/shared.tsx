/**
 * Re-export shared settings primitives so connectors stay aligned with Settings IA.
 * Prefer importing from components/settings/fields in new code.
 */
export {
  Field,
  KeyField,
  SettingsSection,
  SwitchField,
  HelpTip,
  StatusDot,
} from "../../components/settings/fields";
