/**
 * Legacy TitleBar dropdown name. The live launchpad is `AppsLaunchList` in the
 * AI workspace 应用 pane. Re-export so leftover imports still resolve.
 */
export { AppsLaunchList as AppsMenu } from "./AppsLaunchList";
