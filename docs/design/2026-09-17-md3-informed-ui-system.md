# Design System 4.0 — 已落地，不再作为第二份规范

> **状态**：已吸收进现行规范。本文件只留决定，不保留改造过程。

选择性吸收 Material Design 3 的角色色、状态层和完成微动效，不整套换成 MD3。主 CTA 仍是单色 ink。色和密度的数值只在 `topmind-desktop/src/styles/tokens.css`。交互与信息架构只在 `topmind-desktop/DESIGN.md`。

不要在这里补像素、组件清单或实施波次。那些写进 DESIGN 或删掉。
