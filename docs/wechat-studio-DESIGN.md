# 公众号创作 — 实验页已删除

> **状态**：不是交付面，也不是第二份规范。

公众号能力在技能仓 `topmind-wechat`：`SKILL.md`、`DESIGN.md`、`references/` 和 `scripts/`。Desktop 里的「公众号创作」是同一套脚本的伴面，写回仍进工作区文件。

原先的 `docs/experiments/wechat-studio/index.html` 用浏览器 `localStorage` 存草稿，和「文件系统是内容真源」冲突，已删除。不要再做一份脱离工作区文件的公众号编辑器。
